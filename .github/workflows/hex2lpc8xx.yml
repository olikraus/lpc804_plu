name: hex2lpc8xx build

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v1
      - name: Display the path
        run: echo $PWD
        shell: bash
      - name: ls
        run: ls
        shell: bash
