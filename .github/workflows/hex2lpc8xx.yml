name: hex2lpc8xx build

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
      - name: Display the path
        run: echo $PWD
        working-directory: ./lpc804_plu/tools/hex2lpc8xx
        shell: bash
      - name: ls
        run: ls
        shell: bash
